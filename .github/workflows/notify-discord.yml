name: Notify Discord on File Change

on:
  push:
    branches: [main]
    paths:
      - 'Blue Lock Rivals PC.txt' # Указываем путь к файлу

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        env:
          WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          COMMIT_MESSAGE=$(git log -1 --pretty=%B)
          COMMIT_AUTHOR=$(git log -1 --pretty=%an)
          PAYLOAD='{
            "content": "📢 Файл **Blue Lock Rivals PC.txt** был обновлен!\n\nАвтор: **$COMMIT_AUTHOR**\nСообщение: `$COMMIT_MESSAGE`\nСсылка: https://github.com/Abobus307/BlueLock/commit/$GITHUB_SHA"
          }'
          curl -X POST -H "Content-Type: application/json" -d "$PAYLOAD" "$WEBHOOK_URL"
